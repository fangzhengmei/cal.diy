# Cal.diy 应用市场插件接入预约流程分析报告

## 1. 插件注册机制

### 1.1 插件目录结构

应用市场插件统一存储在 `packages/app-store/` 目录下，每个插件拥有独立的文件夹：

```
packages/app-store/
├── zoomvideo/          # Zoom 视频会议插件
├── stripepayment/      # Stripe 支付插件
├── zapier/             # Zapier 集成插件
├── discord/            # Discord 通知插件
├── ga4/                # Google Analytics 4 插件
└── ...                 # 其他插件
```

### 1.2 插件元数据定义

每个插件通过 `_metadata.ts` 文件定义核心元数据，以 Zoom 插件为例：

**文件位置**: `packages/app-store/zoomvideo/_metadata.ts`

```typescript
export const metadata = {
  linkType: "dynamic",
  name: "Zoom Video",
  description: "Zoom 是一个安全可靠的视频平台...",
  type: "zoom_video",              // 插件类型标识
  categories: ["conferencing"],    // 插件分类
  variant: "conferencing",
  logo: "icon.svg",
  publisher: "Cal.diy",
  url: "https://zoom.us/",
  category: "conferencing",
  slug: "zoom",
  title: "Zoom Video",
  email: "help@cal.com",
  appData: {
    location: {
      default: false,
      linkType: "dynamic",          // 动态生成链接
      type: "integrations:zoom",    // 位置类型标识
      label: "Zoom Video",
    },
  },
  dirName: "zoomvideo",
  isOAuth: true,                    // 是否使用 OAuth 认证
} as AppMeta;
```

**支付插件（Stripe）元数据示例**：

**文件位置**: `packages/app-store/stripepayment/_metadata.ts`

```typescript
export const metadata = {
  name: "Stripe",
  description: "支付处理软件...",
  installed: !!(
    process.env.STRIPE_CLIENT_ID &&
    process.env.NEXT_PUBLIC_STRIPE_PUBLIC_KEY &&
    process.env.STRIPE_PRIVATE_KEY
  ),
  slug: "stripe",
  category: "payment",
  categories: ["payment"],
  logo: "icon.svg",
  publisher: "Cal.diy",
  title: "Stripe",
  type: "stripe_payment",
  url: "https://cal.com/",
  docsUrl: "https://stripe.com/docs",
  variant: "payment",
  extendsFeature: "EventType",      // 扩展事件类型功能
  email: "help@cal.com",
  dirName: "stripepayment",
  isOAuth: true,
} as AppMeta;
```

### 1.3 插件注册流程

#### 1.3.1 元数据收集系统

所有插件的元数据通过 `appStoreMetadata` 集中管理，系统启动时自动加载所有插件的 `_metadata.ts` 文件。

#### 1.3.2 应用注册表

**文件位置**: `packages/app-store/_appRegistry.ts`

核心函数：
- `getAppWithMetadata()`: 根据目录名或 slug 获取应用元数据
- `getAppRegistry()`: 获取所有启用的应用列表（前端展示用）
- `getAppRegistryWithCredentials()`: 获取带用户凭证的应用列表

**注册条件**：
1. 应用必须在数据库 `app` 表中存在
2. 应用的 `enabled` 字段必须为 `true`
3. 用户必须拥有有效的凭证（OAuth token 等）

#### 1.3.3 视频适配器动态加载机制

**文件位置**: `packages/app-store/getVideoAdapters.ts`

视频会议插件通过动态导入机制加载，实现插件的热插拔：

```typescript
export const getVideoAdapters = async (withCredentials: CredentialPayload[]): Promise<VideoApiAdapter[]> => {
  const videoAdapters: VideoApiAdapter[] = [];

  for (const cred of withCredentials) {
    // 1. 将凭证类型转换为插件名称（如 "zoom_video" → "zoomvideo"）
    const appName = cred.type.split("_").join("");
    
    // 2. 从 VideoApiAdapterMap 中查找对应的适配器
    let videoAdapterImport = VideoApiAdapterMap[appName as keyof typeof VideoApiAdapterMap];
    
    // 3. 降级处理：尝试去除后缀（如 "zoom_video" → "zoom"）
    if (!videoAdapterImport) {
      const appTypeVariant = cred.type.substring(0, cred.type.lastIndexOf("_"));
      videoAdapterImport = VideoApiAdapterMap[appTypeVariant as keyof typeof VideoApiAdapterMap];
    }

    if (!videoAdapterImport) {
      log.error(`Couldn't get adapter for ${appName}`);
      continue;
    }

    // 4. 动态导入适配器模块
    const videoAdapterModule = await videoAdapterImport;
    const makeVideoApiAdapter = videoAdapterModule.default as VideoApiAdapterFactory;

    // 5. 创建适配器实例并传入凭证
    if (makeVideoApiAdapter) {
      const videoAdapter = makeVideoApiAdapter(cred);
      videoAdapters.push(videoAdapter);
    }
  }

  return videoAdapters;
};
```

**VideoApiAdapterMap** 是一个自动生成的映射表，将插件类型映射到对应的动态导入函数：
- `zoomvideo` → `() => import("./zoomvideo/lib/VideoApiAdapter")`
- `dailyvideo` → `() => import("./dailyvideo/lib/VideoApiAdapter")`
- 等等...

---

## 2. 插件在预约事件各阶段的介入方式

### 2.1 预约事件生命周期阶段

系统通过 **Booking Audit System** 管理预约事件的完整生命周期，定义了以下阶段：

| 阶段 | 描述 | 触发时机 |
|------|------|----------|
| `CREATED` | 预约创建 | 用户成功提交预约请求时 |
| `CANCELLED` | 预约取消 | 组织者或参与者取消预约时 |
| `RESCHEDULED` | 预约重新安排 | 修改预约时间后 |
| `ACCEPTED` | 预约接受 | 组织者确认待处理预约时 |
| `REJECTED` | 预约拒绝 | 组织者拒绝待处理预约时 |
| `RESCHEDULE_REQUESTED` | 重新安排请求 | 参与者请求重新安排时 |
| `ATTENDEE_ADDED` | 添加参与者 | 向现有预约添加新参与者时 |
| `ATTENDEE_REMOVED` | 移除参与者 | 从预约中移除参与者时 |
| `REASSIGNMENT` | 重新分配 | 将预约分配给其他组织者时 |
| `LOCATION_CHANGED` | 位置变更 | 修改预约地点时 |
| `NO_SHOW_UPDATED` | 未出席更新 | 标记参与者未出席时 |
| `SEAT_BOOKED` | 座位预订 | 预订多座位事件的座位时 |
| `SEAT_RESCHEDULED` | 座位重新安排 | 修改座位预订时间时 |

### 2.2 阶段管理机制

**文件位置**: `packages/features/booking-audit/lib/service/BookingAuditActionServiceRegistry.ts`

系统使用 **注册中心模式** 管理各阶段的处理服务：

```typescript
export class BookingAuditActionServiceRegistry {
  private readonly actionServices: Map<BookingAuditAction, IAuditActionService>;

  constructor() {
    // 注册各阶段对应的处理服务
    const services: Array<[BookingAuditAction, IAuditActionService]> = [
      ["CREATED", new CreatedAuditActionService()],
      ["CANCELLED", new CancelledAuditActionService()],
      ["RESCHEDULED", new RescheduledAuditActionService()],
      ["ACCEPTED", new AcceptedAuditActionService()],
      ["REJECTED", new RejectedAuditActionService()],
      // ... 其他阶段
    ];
    this.actionServices = new Map(services);
  }

  getActionService(action: BookingAuditAction): IAuditActionService {
    const service = this.actionServices.get(action);
    if (!service) {
      throw new Error(`No action service found for: ${action}`);
    }
    return service;
  }
}
```

### 2.3 视频会议插件的介入时机

视频会议插件通过 **VideoApiAdapter** 接口在预约的关键阶段介入：

#### 2.3.1 核心接口定义

每个视频会议插件必须实现 `VideoApiAdapter` 接口：

```typescript
interface VideoApiAdapter {
  // 创建会议 - 预约创建阶段调用
  createMeeting(event: CalendarEvent): Promise<VideoCallData>;
  
  // 更新会议 - 预约修改阶段调用
  updateMeeting(bookingRef: PartialReference, event: CalendarEvent): Promise<VideoCallData>;
  
  // 删除会议 - 预约取消阶段调用
  deleteMeeting(uid: string): Promise<void>;
  
  // 获取可用性 - 检查用户忙闲状态（可选）
  getAvailability?(): Promise<EventBusyDate[]>;
}
```

#### 2.3.2 创建阶段介入

**文件位置**: `packages/features/conferencing/lib/videoClient.ts`

```typescript
const createMeeting = async (
  credential: CredentialPayload | CredentialForCalendarService,
  calEvent: CalendarEvent
) => {
  // 1. 验证凭证和应用状态
  if (!credential || !credential.appId) {
    throw new Error("Credentials must be set!");
  }

  // 2. 检查应用是否启用
  const enabledApp = await prisma.app.findUnique({
    where: { slug: credential.appId },
    select: { enabled: true },
  });

  if (!enabledApp?.enabled) {
    throw `Location app ${credential.appId} is either disabled or not seeded at all`;
  }

  // 3. 获取对应的视频适配器
  const videoAdapters = await getVideoAdapters([credential]);
  const [firstVideoAdapter] = videoAdapters;

  // 4. 调用适配器创建会议
  try {
    const createdMeeting = await firstVideoAdapter?.createMeeting(calEvent);
    return {
      ...returnObject,
      createdEvent: createdMeeting,
      success: true,
    };
  } catch (err) {
    // 5. 失败处理：发送故障通知邮件，降级到 Cal Video
    await sendBrokenIntegrationEmail(calEvent, "video");
    const defaultMeeting = await createMeetingWithCalVideo(calEvent);
    return { ...returnObject, createdEvent: defaultMeeting };
  }
};
```

#### 2.3.3 Zoom 适配器实现示例

**文件位置**: `packages/app-store/zoomvideo/lib/VideoApiAdapter.ts`

```typescript
const ZoomVideoApiAdapter = (credential: CredentialPayload): VideoApiAdapter => {
  // 1. 从凭证中获取 OAuth token
  const tokenResponse = getTokenObjectFromCredential(credential);

  // 2. 定义 API 请求封装（处理 token 刷新）
  const fetchZoomApi = async (endpoint: string, options?: RequestInit) => {
    const auth = new OAuthManager({
      credentialSyncVariables: { /* 凭证同步配置 */ },
      resourceOwner: { type: "user", id: credential.userId },
      appSlug: metadata.slug,
      currentTokenObject: tokenResponse,
      // 自动刷新 token 的回调
      fetchNewTokenObject: async ({ refreshToken }) => {
        // 调用 Zoom OAuth API 刷新 token
      },
      // 检查 token 是否失效
      isAccessTokenUnusable: async function (response) {
        // 检查响应状态码和错误信息
      },
      // 更新数据库中的 token
      updateTokenObject: async (newTokenObject) => {
        await prisma.credential.update({
          where: { id: credential.id },
          data: { key: newTokenObject as Prisma.InputJsonValue },
        });
      },
    });

    const { json } = await auth.request({
      url: `https://api.zoom.us/v2/${endpoint}`,
      options,
    });
    return json;
  };

  // 3. 实现适配器接口
  return {
    getAvailability: async () => {
      // 获取用户已安排的会议，用于忙闲检测
      const responseBody = await fetchZoomApi("users/me/meetings?type=scheduled");
      return data.meetings.map((meeting) => ({
        start: meeting.start_time,
        end: new Date(...).toISOString(),
      }));
    },

    createMeeting: async (event: CalendarEvent): Promise<VideoCallData> => {
      // 转换预约事件为 Zoom 会议格式
      const translatedEvent = await translateEvent(event);
      
      // 调用 Zoom API 创建会议
      const response = await fetchZoomApi("users/me/meetings", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(translatedEvent),
      });

      const result = zoomEventResultSchema.parse(response);
      
      // 返回会议链接等信息
      return {
        type: "zoom_video",
        id: result.id.toString(),
        password: result.password || "",
        url: result.join_url,
      };
    },

    deleteMeeting: async (uid: string): Promise<void> => {
      await fetchZoomApi(`meetings/${uid}`, { method: "DELETE" });
    },

    updateMeeting: async (bookingRef: PartialReference, event: CalendarEvent): Promise<VideoCallData> => {
      // 1. 更新会议信息
      await fetchZoomApi(`meetings/${bookingRef.uid}`, {
        method: "PATCH",
        body: JSON.stringify(await translateEvent(event)),
      });
      
      // 2. 获取更新后的会议信息
      const updatedMeeting = await fetchZoomApi(`meetings/${bookingRef.uid}`);
      return { /* 返回更新后的会议数据 */ };
    },
  };
};
```

### 2.4 其他类型插件的介入方式

#### 2.4.1 Webhook 通知插件（如 Zapier）

通过任务队列系统在预约事件发生时触发 webhook：

**文件位置**: `packages/features/tasker/tasks/sendWebook.ts`

```typescript
export async function sendWebhook(payload: string): Promise<void> {
  const { secretKey, triggerEvent, createdAt, webhook, data } = sendWebhookPayloadSchema.parse(
    JSON.parse(payload)
  );
  
  // 发送 webhook 通知到订阅者 URL
  await sendPayload(secretKey, triggerEvent, createdAt, webhook, data);
}
```

#### 2.4.2 CRM 集成插件

通过异步任务在预约创建后同步到 CRM 系统：

**文件位置**: `packages/features/tasker/tasks/crm/createCRMEvent.ts`

---

## 3. 插件配置数据如何影响核心预约逻辑

### 3.1 配置数据存储结构

#### 3.1.1 存储位置

插件配置存储在 **事件类型（EventType）** 的 `metadata` 字段中，JSON 结构如下：

```typescript
{
  "apps": {
    "stripe": {
      "enabled": true,
      "price": 5000,           // 50.00 美元（以分为单位）
      "currency": "usd",
      "paymentOption": "ON_BOOKING"
    },
    "ga4": {
      "enabled": true,
      "TRACKING_ID": "G-XXXXXXXXXX",
      "trackingEvent": "Lead"
    },
    "giphy": {
      "enabled": true,
      "thankYouPage": "https://..."
    }
  },
  // 其他元数据...
}
```

#### 3.1.2 配置数据获取工具

**文件位置**: `packages/app-store/_utils/getEventTypeAppData.ts`

```typescript
export const getEventTypeAppData = <T extends EventTypeAppsList>(
  eventType: BookerEventForAppData,
  appId: T,
  forcedGet?: boolean
): EventTypeApps[T] | null => {
  const metadata = eventType.metadata;
  const appMetadata = metadata?.apps && metadata.apps[appId];
  
  if (appMetadata) {
    const app = appMetadata as CommonAppData;
    // 只有启用的插件才返回配置（除非强制获取）
    const allowDataGet = forcedGet ? true : app.enabled;
    
    return allowDataGet
      ? {
          ...appMetadata,
          // 优先级：eventType.price > appMetadata.price
          price: eventType.price || app.price || null,
          currency: eventType.currency || app.currency || null,
          // 兼容旧字段名
          TRACKING_ID: app.TRACKING_ID || app.trackingId || null,
          TRACKING_EVENT: app.trackingEvent || "Lead",
        }
      : null;
  }

  // 向后兼容：旧版本的配置存储方式
  const legacyAppsData = {
    stripe: {
      enabled: !!eventType.price,
      price: eventType.price,
      currency: eventType.currency,
      paymentOption: "ON_BOOKING",
    },
    giphy: {
      enabled: !!eventType.metadata?.giphyThankYouPage,
      thankYouPage: eventType.metadata?.giphyThankYouPage || "",
    },
  } as const;

  const legacyAppData = legacyAppsData[appId as Extract<T, keyof typeof legacyAppsData>];
  const allowDataGet = forcedGet ? true : legacyAppData?.enabled;
  return allowDataGet ? legacyAppData : null;
};
```

### 3.2 配置对预约流程的影响

#### 3.2.1 支付插件（Stripe）的影响

**配置字段**：
- `enabled`: 是否启用支付
- `price`: 预约价格（以最小货币单位计，如美分）
- `currency`: 货币类型（如 "usd", "eur"）
- `paymentOption`: 支付时机（"ON_BOOKING" - 预订时支付）

**对流程的影响**：

1. **预约创建阶段**：
   - 如果 `enabled=true` 且 `price>0`，系统会在预约确认前跳转 Stripe 支付页面
   - 支付成功后才会创建实际的预约记录
   - 支付金额和货币由配置决定

2. **预约验证阶段**：
   - 检查用户是否有有效的 Stripe 凭证
   - 验证价格和货币配置是否有效

#### 3.2.2 视频会议插件的影响

**配置存储方式**：
视频会议插件的配置通过 **位置类型（Location Type）** 系统集成：

1. **插件元数据定义**：
   ```typescript
   appData: {
     location: {
       type: "integrations:zoom",    // 位置类型标识
       linkType: "dynamic",          // 动态生成会议链接
       label: "Zoom Video",
     }
   }
   ```

2. **事件类型配置**：
   用户在事件类型设置中选择可用的位置选项，配置存储在 `eventType.locations` 数组中：
   ```typescript
   [
     {
       type: "integrations:zoom",     // 选择 Zoom 作为会议方式
       credentialId: 123,             // 用户的 Zoom 凭证 ID
     }
   ]
   ```

**对预约流程的影响**：

1. **预约创建时**：
   - 根据 `location.type` 确定使用哪个视频适配器
   - 根据 `credentialId` 加载用户的 OAuth 凭证
   - 调用适配器的 `createMeeting()` 生成会议链接
   - 会议链接会保存到预约记录中

2. **预约确认邮件**：
   - 邮件模板会包含生成的 Zoom 会议链接
   - 链接类型（静态/动态）由 `linkType` 决定

#### 3.2.3 分析追踪插件（GA4）的影响

**配置字段**：
- `enabled`: 是否启用追踪
- `TRACKING_ID`: Google Analytics 测量 ID
- `TRACKING_EVENT`: 事件名称（默认 "Lead"）

**对流程的影响**：

1. **预约成功页面**：
   - 加载 GA4 脚本
   - 发送配置的追踪事件（如 `Lead`）
   - 事件包含预约金额、货币等参数

2. **数据层集成**：
   - 将预约信息推送到 dataLayer
   - 可用于自定义追踪和报告

### 3.3 配置优先级和合并规则

#### 3.3.1 价格配置优先级

在 `getEventTypeAppData()` 中，价格配置遵循以下优先级：

```
eventType.price（事件类型直接设置）
  > appMetadata.price（插件配置中的价格）
  > null（无价格）
```

这允许：
- 全局插件默认价格
- 特定事件类型覆盖价格

#### 3.3.2 向后兼容策略

系统维护了旧版本配置的向后兼容：

| 旧版本存储位置 | 新版本存储位置 | 兼容处理 |
|----------------|----------------|----------|
| `eventType.price` | `metadata.apps.stripe.price` | 两者都支持，优先使用新位置 |
| `eventType.metadata.giphyThankYouPage` | `metadata.apps.giphy.thankYouPage` | 自动迁移读取 |
| `trackingId` | `TRACKING_ID` | 两个字段名都支持 |

### 3.4 插件扩展事件类型的方式

#### 3.4.1 位置选项扩展

视频会议插件通过 `appData.location` 扩展事件类型的位置选项：

**文件位置**: `packages/app-store/locations.ts`

```typescript
// 1. 从所有插件元数据中提取位置配置
for (const [appName, meta] of Object.entries(appStoreMetadata)) {
  const location = meta.appData?.location;
  if (location) {
    // 2. 处理模板变量（如 {SLUG}, {TITLE}）
    for (const [key, value] of Object.entries(location)) {
      if (typeof value === "string") {
        location[key] = value.replace(/{SLUG}/g, meta.slug).replace(/{TITLE}/g, meta.name);
      }
    }

    // 3. 根据链接类型设置输入方式
    if (newLocation.linkType === "static") {
      // 静态链接：需要组织者手动输入
      newLocation.organizerInputType = location.organizerInputType || "text";
    } else {
      // 动态链接：系统自动生成，无需输入
      newLocation.organizerInputType = null;
    }

    // 4. 注册到全局位置类型映射
    AppStoreLocationType[appName] = newLocation.type;
    locationsFromApps.push({ ...newLocation });
  }
}

// 5. 合并默认位置和插件位置
const locations = [...defaultLocations, ...locationsFromApps];
```

#### 3.4.2 事件类型功能扩展

支付插件通过 `extendsFeature: "EventType"` 标记扩展事件类型的功能：

- 在事件类型编辑页面显示额外的配置表单
- 配置项存储在 `metadata.apps[pluginId]` 中
- 预约流程中根据配置决定是否触发插件逻辑

---

## 4. 总结

### 4.1 插件接入核心机制

| 机制 | 实现方式 | 关键文件 |
|------|----------|----------|
| **插件注册** | 元数据 + 动态导入 | `_metadata.ts`, `getVideoAdapters.ts` |
| **生命周期介入** | 阶段注册中心 + 适配器模式 | `BookingAuditActionServiceRegistry.ts`, `videoClient.ts` |
| **配置管理** | 事件类型 metadata 存储 | `getEventTypeAppData.ts` |
| **凭证管理** | OAuth 2.0 + 自动刷新 | `OAuthManager.ts`, 各插件的 `VideoApiAdapter.ts` |

### 4.2 插件类型与介入时机

| 插件类型 | 示例 | 介入阶段 | 核心接口 |
|----------|------|----------|----------|
| **视频会议** | Zoom, Google Meet, Teams | 创建/更新/取消 | `VideoApiAdapter` |
| **支付** | Stripe, PayPal | 预约确认前 | `extendsFeature: "EventType"` |
| **CRM** | HubSpot, Close.com | 预约创建后（异步） | 任务队列 |
| **通知** | Discord, Slack | 各事件阶段 | Webhook / 任务队列 |
| **分析** | GA4, PostHog | 预约成功页面 | 前端脚本注入 |

### 4.3 配置影响预约流程的关键路径

```
用户创建预约
    ↓
检查事件类型配置
    ↓
┌─────────────────────────────────────────┐
│  检查是否启用支付插件                    │
│  ├─ 是：跳转 Stripe 支付页面            │
│  │   └─ 支付成功后继续                   │
│  └─ 否：直接继续                         │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  检查位置类型配置                        │
│  ├─ 动态视频链接（如 Zoom）             │
│  │   ├─ 加载用户凭证                     │
│  │   ├─ 调用视频适配器 createMeeting()  │
│  │   └─ 生成会议链接                     │
│  ├─ 静态链接                             │
│  │   └─ 使用用户预设的链接               │
│  └─ 其他位置（电话、地址等）             │
│      └─ 直接使用配置值                   │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  预约创建完成                            │
│  ├─ 发送确认邮件（含会议链接）           │
│  ├─ 触发分析追踪（GA4 等）               │
│  ├─ 异步同步到 CRM                       │
│  └─ 触发 webhook 通知（Zapier 等）       │
└─────────────────────────────────────────┘
```

### 4.4 设计亮点

1. **高度可扩展的插件系统**：
   - 基于元数据的声明式配置
   - 动态导入实现按需加载
   - 适配器模式统一接口

2. **完善的错误处理和降级**：
   - 视频插件失败时自动降级到 Cal Video
   - OAuth token 自动刷新机制
   - 故障通知邮件

3. **向后兼容策略**：
   - 配置迁移支持
   - 多字段名兼容
   - 优雅的版本迁移

4. **安全考虑**：
   - 凭证加密存储
   - API key 不暴露到前端
   - PII（个人身份信息）脱敏日志

---

*报告生成时间：2026-05-02*
